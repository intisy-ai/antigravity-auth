node_modules/
dist/
build/
*/build/
.gradle/
# TeaVM-compiled orchestrator ESM staged by teavm-build.mjs at build time, never committed.
# The .d.ts emitted beside it from the same Java IS committed, so the TypeScript seam type-checks
# on a clean clone before anything is built.
src/generated/antigravity-orchestrator.teavm.js
.DS_Store
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea/
package-lock.json
.superpowers/
docs/
graphify-out/
logs/

# Generated on the default branch by the release readme job.
README.md
