{
  "name": "antigravity-auth",
  "version": "1.10.0",
  "description": "Google Antigravity provider for OpenCode and Claude Code, built on core-auth (multi-account rotation, quota management, proxy support)",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "antigravity": "dist/cli.js"
  },
  "license": "MIT",
  "author": "intisy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/intisy-ai/antigravity-auth.git"
  },
  "keywords": [
    "opencode",
    "claude",
    "antigravity",
    "google",
    "oauth",
    "gemini",
    "core-auth",
    "provider"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "dist/",
    "cairn.json",
    "icon.svg",
    "README.md",
    "LICENSE"
  ],
  "claudeHub": {
    "authProviders": [
      {
        "name": "antigravity",
        "handler": "dist/handler.js",
        "models": [],
        "accountPool": "antigravity"
      },
      {
        "name": "gemini-cli",
        "handler": "dist/handler.js",
        "models": [],
        "accountPool": "antigravity"
      }
    ]
  },
  "scripts": {
    "build": "npm --prefix core-ir install && npm --prefix core-ir run build && node core/teavm-build.mjs --java-dir java --module :antigravity-teavm --out src/generated/antigravity-orchestrator.teavm.js && npm --prefix core-auth run build:teavm && npx tsc --project core-auth/tsconfig.json && node core-auth/copy-generated.mjs --root core-auth && node build.mjs",
    "postbuild": "node dist/index.js readme",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@openauthjs/openauth": "^0.4.3",
    "@intisy-ai/core": "file:core",
    "@intisy-ai/core-auth": "file:core-auth"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "esbuild": "^0.25.12",
    "typescript": "^5.0.0",
    "vitest": "^4.1.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
