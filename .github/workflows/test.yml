name: Test

on: [push, pull_request, workflow_dispatch]

permissions:
  contents: read
  packages: read

jobs:
  test:
    uses: intisy/workflows/.github/workflows/test.yml@main
    with:
      post_test: 'node antigravity-teavm/smoke/orchestrator-async-smoke.mjs'
    secrets:
      envPAT: ${{ secrets.PAT }}
