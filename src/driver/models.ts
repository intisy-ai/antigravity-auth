// @ts-nocheck
// The model catalog exposed to the loaders. Already in core-auth ProviderModel
// shape (name + limit + modalities + variants), so it passes straight through.

export { OPENCODE_MODEL_DEFINITIONS as models } from "../plugin/config/models.js";
